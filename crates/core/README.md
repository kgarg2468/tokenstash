# tokenstash-core

Library behind the [`tokenstash`](https://github.com/kgarg2468/tokenstash) CLI: the stash adapters, the grant ledger, the provider registry and delivery into a project's env file. Published so `cargo install tokenstash` works; the public API is not stable and follows the CLI's version.
