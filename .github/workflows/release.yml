name: release
on:
  push:
    tags: ["v*"]
permissions: { contents: write }
jobs:
  test:
    # A tag on an untested commit must not publish. Same gate as ci.yml, minus the leak test
    # (the release binaries are checked below).
    runs-on: ubuntu-latest
    permissions: { contents: read }
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with: { toolchain: stable, components: clippy }
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
      - run: cargo clippy --workspace --all-targets --all-features -- -D warnings
      - run: cargo test --workspace --locked
  release:
    # Created once, as a DRAFT, so the four build jobs never race on creating the same
    # release and `releases/latest` keeps pointing at the previous version until every
    # artifact and every registry step has succeeded (see `publish` at the bottom).
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: tag matches the workspace version
        # v0.2.0 or v0.2.0-rc.1 (a pre-release dry run: npm/PyPI get the rc version, the
        # binary still reports 0.2.0; crates and the tap are skipped for pre-releases).
        run: |
          tag="${GITHUB_REF_NAME#v}"; base="${tag%%-*}"
          cargo=$(grep -m1 '^version = ' Cargo.toml | sed 's/version = "\(.*\)"/\1/')
          [ "$base" = "$cargo" ] || { echo "tag $GITHUB_REF_NAME does not match Cargo.toml version $cargo"; exit 1; }
          # The same pre-release grammar PyPI accepts (scripts/pypi-wheels.py): a suffix that
          # passed here and failed there used to leave a half-published release.
          case "$tag" in
            *-*) echo "$tag" | grep -Eq -- '-(rc|a|b|alpha|beta)\.[0-9]+$' || { echo "pre-release tag must end in -rc.N, -a.N, -b.N, -alpha.N or -beta.N"; exit 1; } ;;
          esac
      - env: { GH_TOKEN: "${{ github.token }}" }
        run: |
          pre=""; case "$GITHUB_REF_NAME" in *-*) pre="--prerelease" ;; esac
          gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1 || gh release create "$GITHUB_REF_NAME" $pre --draft --generate-notes --notes "See CHANGELOG.md for what changed and how the upgrade behaves."
  build:
    needs: release
    strategy:
      matrix:
        include:
          # Linux builds are static (musl): the glibc builds required glibc 2.39 and would not
          # start on Ubuntu 22.04 / Debian 12 / Amazon Linux 2023. arm64 builds natively on the
          # free public-repo arm runner (no cross).
          - { os: ubuntu-latest,    target: x86_64-unknown-linux-musl,  name: linux-x64,   musl: true, ccvar: CC_x86_64_unknown_linux_musl,  cc: x86_64-linux-musl-gcc }
          - { os: ubuntu-24.04-arm, target: aarch64-unknown-linux-musl, name: linux-arm64, musl: true, ccvar: CC_aarch64_unknown_linux_musl, cc: aarch64-linux-musl-gcc }
          - { os: macos-latest,    target: aarch64-apple-darwin,       name: darwin-arm64 }
          - { os: macos-15-intel,  target: x86_64-apple-darwin,        name: darwin-x64 }
    runs-on: ${{ matrix.os }}
    permissions: { contents: write, id-token: write, attestations: write }
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with: { toolchain: stable, targets: "${{ matrix.target }}" }
      - if: matrix.musl
        # musl-tools installs <arch>-linux-musl-gcc; name it explicitly so cc-rs (bundled
        # SQLite) never falls back to the host glibc compiler.
        run: |
          sudo apt-get update -q && sudo apt-get install -y -q musl-tools
          command -v ${{ matrix.cc }}
          echo "${{ matrix.ccvar }}=${{ matrix.cc }}" >> "$GITHUB_ENV"
      - run: cargo build --release --locked --target ${{ matrix.target }}
      - name: binary sanity (static on Linux, runs everywhere)
        run: |
          bin=target/${{ matrix.target }}/release/tokenstash
          "$bin" --version
          if [ "${{ matrix.musl }}" = "true" ]; then ldd "$bin" 2>&1 | grep -q 'statically linked\|not a dynamic executable'; fi
      - name: package + checksum
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/tokenstash dist/
          tar -C dist -czf tokenstash-${{ matrix.name }}.tar.gz tokenstash
          shasum -a 256 tokenstash-${{ matrix.name }}.tar.gz > tokenstash-${{ matrix.name }}.tar.gz.sha256
      # Signed provenance for the tarball itself: `gh attestation verify tokenstash-*.tar.gz
      # --repo kgarg2468/tokenstash` proves it was built by this workflow from this commit.
      # npm and PyPI carry their own; the direct-download and brew paths had nothing.
      - uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with: { subject-path: "tokenstash-${{ matrix.name }}.tar.gz" }
      - env: { GH_TOKEN: "${{ github.token }}" }
        run: gh release upload "$GITHUB_REF_NAME" tokenstash-${{ matrix.name }}.tar.gz tokenstash-${{ matrix.name }}.tar.gz.sha256 --clobber
      # The same bytes, kept inside this run. Release assets are mutable by anyone holding
      # contents: write, so publishing from them would let a clobber in the window between
      # this upload and the npm/PyPI download ship an attacker's binary under our provenance.
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with: { name: "binary-${{ matrix.name }}", path: "tokenstash-${{ matrix.name }}.tar.gz*", retention-days: 7 }
  npm:
    # Five packages: four platform packages (binary only, `os`/`cpu` pinned, no scripts) and
    # the `tokenstash` launcher that lists them as optionalDependencies. No postinstall, so
    # bun / pnpm 10 / --ignore-scripts installs work.
    # Bootstrap (first release): npm trusted publishers can only be configured on packages
    # that exist, so set NPM_TOKEN to a granular token (publish, bypass 2FA, short expiry).
    # Then configure a trusted publisher on all five packages (this repo, release.yml), set
    # the repo variable NPM_TRUSTED_PUBLISHING=true and delete NPM_TOKEN: OIDC with automatic
    # provenance from then on. Never unpublish a version: name@version can never be reused
    # and the launcher pins exact versions; ship a patch release instead.
    needs: build
    runs-on: ubuntu-latest
    permissions: { contents: read, id-token: write }   # publishes; never writes to the repo
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with: { node-version: 22 }   # no registry-url: it would write an .npmrc with an empty _authToken and break OIDC
      - run: npm install -g npm@11.17.0 && npm --version   # trusted publishing needs ≥ 11.5.1; pinned on purpose
      - name: take the binaries from this run, not from the mutable release
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with: { pattern: "binary-*", merge-multiple: true, path: /tmp/dist }
      - name: npm publish gate
        id: gate
        env: { T: "${{ secrets.NPM_TOKEN }}", OIDC: "${{ vars.NPM_TRUSTED_PUBLISHING }}" }
        run: |
          if [ -n "$T" ]; then
            echo "//registry.npmjs.org/:_authToken=$T" > ~/.npmrc; chmod 600 ~/.npmrc; echo "publish=true" >> "$GITHUB_OUTPUT"
          elif [ "$OIDC" = "true" ]; then
            echo "publish=true" >> "$GITHUB_OUTPUT"
          else
            echo "publish=false" >> "$GITHUB_OUTPUT"; echo "::notice::neither NPM_TOKEN nor NPM_TRUSTED_PUBLISHING set; npm skipped"
          fi
      - name: assemble (and publish when gated on)
        env: { NPM_PUBLISH: "${{ steps.gate.outputs.publish == 'true' && '1' || '' }}" }
        run: scripts/npm-package.sh "${GITHUB_REF_NAME#v}" /tmp/dist /tmp/npm-out
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with: { name: npm-packages, path: /tmp/npm-out, retention-days: 14 }
  crates:
    # Gated on CARGO_REGISTRY_TOKEN (crates.io trusted publishing is not used here). Without
    # the secret the job is skipped and `cargo install tokenstash` keeps working from --git.
    # Never for a pre-release tag: Cargo.toml carries the final version.
    needs: build
    if: ${{ !contains(github.ref_name, '-') }}
    runs-on: ubuntu-latest
    permissions: { contents: read }
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with: { toolchain: stable }
      - name: crates.io publish gate
        id: gate
        env: { T: "${{ secrets.CARGO_REGISTRY_TOKEN }}" }
        run: |
          if [ -n "$T" ]; then echo "publish=true" >> "$GITHUB_OUTPUT"; else echo "publish=false" >> "$GITHUB_OUTPUT"; echo "::notice::CARGO_REGISTRY_TOKEN not set; skipping crates.io"; fi
      - if: steps.gate.outputs.publish == 'true'
        # Token scopes: publish-new (first release of each crate) + publish-update. Each crate
        # is skipped when this version is already on crates.io, so a re-run after a later job
        # failed does not abort here. `cargo publish` blocks until the crate is in the index,
        # so the CLI (which depends on the published core) can follow directly; it is not
        # --locked because cargo rewrites the path dependency for the verification build.
        run: |
          v="${GITHUB_REF_NAME#v}"
          # 200 = already there, 404 = not yet; anything else is crates.io or the network,
          # which must not be mistaken for "not published" and turned into a failing publish.
          published() {
            code=$(curl -s -o /dev/null -w '%{http_code}' -H 'User-Agent: tokenstash-release (github.com/kgarg2468/tokenstash)' "https://crates.io/api/v1/crates/$1/$2")
            case "$code" in 200) return 0 ;; 404) return 1 ;; *) echo "crates.io answered $code for $1 $2"; exit 1 ;; esac
          }
          published tokenstash-core "$v" || cargo publish -p tokenstash-core --locked
          published tokenstash "$v" || cargo publish -p tokenstash
        env: { CARGO_REGISTRY_TOKEN: "${{ secrets.CARGO_REGISTRY_TOKEN }}" }
  pypi:
    # Wheels are the release tarballs repacked (binary under .data/scripts, no Python code):
    # `pipx install tokenstash` / `uv tool install tokenstash`. Published with PyPI trusted
    # publishing: a pending publisher for kgarg2468/tokenstash, workflow release.yml,
    # environment `pypi`, creates the project on first use; no token. Gated on the repo
    # variable PYPI_TRUSTED_PUBLISHING=true so the run stays green until that is configured
    # (GitHub creates the environment on first reference, so the environment is no gate).
    needs: build
    if: ${{ vars.PYPI_TRUSTED_PUBLISHING == 'true' }}
    runs-on: ubuntu-latest
    environment: pypi
    permissions: { contents: read, id-token: write }
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: take the binaries from this run, not from the mutable release
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with: { pattern: "binary-*", merge-multiple: true, path: /tmp/dist }
      - run: python3 scripts/pypi-wheels.py "${GITHUB_REF_NAME#v}" /tmp/dist dist
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with: { name: wheels, path: dist, retention-days: 14 }
      - uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # v1.14.2
        with: { packages-dir: dist, verbose: true, skip-existing: true }
  brew:
    # Formula from the release digests, attached to the release and pushed to the tap
    # (kgarg2468/homebrew-tokenstash — must be public). TAP_TOKEN: a fine-grained PAT with
    # contents: write on the tap repo only; without it the formula is still attached to the
    # release for a manual paste. Never for a pre-release tag.
    needs: build
    if: ${{ !contains(github.ref_name, '-') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: take the digests from this run, not from the mutable release
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with: { pattern: "binary-*", merge-multiple: true, path: /tmp/dist }
      - env: { GH_TOKEN: "${{ github.token }}", SUMS_DIR: /tmp/dist }
        run: |
          bash scripts/brew-formula.sh "$GITHUB_REF_NAME" > tokenstash.rb
          ruby -c tokenstash.rb
          cat tokenstash.rb
          gh release upload "$GITHUB_REF_NAME" tokenstash.rb --clobber
      - name: push to the tap
        env: { TAP_TOKEN: "${{ secrets.TAP_TOKEN }}" }
        # The token travels in a header, never in the remote URL or on disk.
        run: |
          if [ -z "$TAP_TOKEN" ]; then echo "::notice::TAP_TOKEN not set; formula attached to the release only"; exit 0; fi
          auth="AUTHORIZATION: bearer $TAP_TOKEN"
          git -c http.extraheader="$auth" clone -q --depth 1 https://github.com/kgarg2468/homebrew-tokenstash.git /tmp/tap
          mkdir -p /tmp/tap/Formula && cp tokenstash.rb /tmp/tap/Formula/tokenstash.rb
          cd /tmp/tap && git add Formula/tokenstash.rb
          if git diff --cached --quiet; then echo "formula unchanged"; exit 0; fi
          git -c user.name=tokenstash-release -c user.email=noreply@github.com commit -qm "tokenstash $GITHUB_REF_NAME"
          git -c http.extraheader="$auth" push -q origin HEAD
  publish:
    # Flip the draft to a real release only when everything that was supposed to run did.
    # A failed npm or PyPI step leaves the draft in place: `releases/latest` still points at
    # the previous version, and re-running the failed jobs completes the set.
    needs: [build, npm, crates, pypi, brew]
    if: ${{ always() }}
    runs-on: ubuntu-latest
    steps:
      - env:
          GH_TOKEN: "${{ github.token }}"
          RESULTS: "${{ join(needs.*.result, ' ') }}"
        run: |
          for r in $RESULTS; do
            case "$r" in success|skipped) ;; *) echo "a release job ended with '$r'; leaving the release as a draft"; exit 1 ;; esac
          done
          gh release edit "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" --draft=false
          echo "published $GITHUB_REF_NAME"
